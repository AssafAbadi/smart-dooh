# Project context (for future reference)

- **MVP**: This project is an MVP. Prefer simple, single-path implementations. Avoid backward-compatibility layers or legacy support unless explicitly needed.
- **No existing clients**: There are no production or external clients. Schema and API changes do not need to preserve old formats; you can remove deprecated fields and endpoints when simplifying.
